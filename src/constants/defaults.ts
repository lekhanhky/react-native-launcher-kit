/** Default fallback values used when native calls fail. */
export const Defaults = {
  /** Default battery status returned on error. */
  BATTERY_STATUS: {
    level: 0,
    isCharging: false,
  },
  /** Empty string fallback. */
  EMPTY_STRING: '',
};
